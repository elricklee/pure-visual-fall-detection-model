乙的INT8量化与验证任务已完成并提交。

本次基于甲的7月26日FP32 ONNX完成静态INT8 QDQ逐通道量化，正式模型为：

`artifacts/onnx/fall_pose_384_int8_qdq.onnx`

模型由13.3421MB缩小到3.6931MB，体积减少72.32%。本次同环境PC CPU参考测试中，平均推理延迟由24.3201ms降到19.0201ms，降低21.79%；该数据仅为PC参考值，不是NPU实测。

11张小验证集上，可见光Pose mAP50-95由0.53376变为0.54595，伪红外由0.39625变为0.39577，量化精度变化控制在2个百分点以内。10段无真值视频中，可见光告警结论10/10一致，伪红外9/10一致；差异视频属于只触发1帧的边界样本，需补充真值后判断。

已同步提交量化脚本、模型配置、算子审计、FP32/INT8测速、姿态精度对比、双模式视频报告和NPU可行性说明。真实红外相机效果、NPU端到端延迟和NPU运行内存仍需在后续板卡与真实数据到位后验证。
